# Build output
unifi-smash-deck
unifi-smash-deck.log
unifi-smash-deck.pid

# Runtime data — settings, automations, logs, and camera snapshots all contain
# user-specific or potentially sensitive information; never commit them.
data/*.json
data/*.log
data/snapshots/
data/

# Local runtime artifacts and model weights
*.log
*.bak
*.old
honeypot-agent
unifideck
sidecar/__pycache__/
sidecar/*.pt

# Local env overrides (may contain API keys, IP addresses, etc.)
.env
.env.*
!.env.example
docs/screenshots/*.png
docs/screenshots/*.jpg
docs/screenshots/*.jpeg
